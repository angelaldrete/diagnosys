import PriorityMessageBase from "../types/PriorityMessage";

const usePriorityMessage = () => {
  const priorityMessage: PriorityMessageBase = {
    message: "",
    title: "",
  };

  return priorityMessage;
};

export default usePriorityMessage;
